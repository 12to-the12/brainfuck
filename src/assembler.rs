// use std::fs::File;
// use std::io::BufReader;

// pub fn read_file_string(filepath: &str) -> Result<String, Box<dyn std::error::Error>> {
    // let data = fs::read_to_string(filepath)?;
    // Ok(data)
// 
// }
// 
// fn transpile(assembly: &str) -> str {
    // replaces shorthand
    // removes comments and maps labels
    // also structures each line as a struct
// }